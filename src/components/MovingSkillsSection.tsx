import React from 'react';

const MovingSkillsSection = () => {
  // This component is now empty since the skills have been moved to PersonalSection
  // Keeping it as a placeholder in case it's needed later
  return null;
};

export default MovingSkillsSection;
